// GovernmentJobs component — renders the full GovernmentExams page
// This ensures both old cached code AND new code show the same content
import GovernmentExamsPage from '@/pages/GovernmentExams';

export const GovernmentJobs = () => {
  return <GovernmentExamsPage />;
};

export default GovernmentJobs;
